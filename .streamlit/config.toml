# App theme. Custom CSS in obs_tune/ui.py layers on top of this base.
[theme]
base = "light"
primaryColor = "#5b4ae0"
backgroundColor = "#f7f4ff"
secondaryBackgroundColor = "#f1ecff"
textColor = "#201a33"
font = "sans serif"

[client]
toolbarMode = "minimal"

[browser]
gatherUsageStats = false
